# 个人网站使用说明

## 🌐 网站地址

- **主页**: https://me-pfam5sz07-yuyu0308s-projects.vercel.app
- **编辑器**: https://me-pfam5sz07-yuyu0308s-projects.vercel.app/edit.html

## 📝 如何编辑网站内容

### 方法1：使用在线编辑器（推荐）

1. 访问：https://me-pfam5sz07-yuyu0308s-projects.vercel.app/edit.html
2. 编辑你想要修改的内容
3. 点击"下载data.json"按钮
4. 将下载的文件放到项目根目录 `C:\Users\lenovo\personal-website\data.json`
5. 推送到GitHub（见下方）
6. 1-2分钟后网站自动更新

### 方法2：使用GitHub Web界面

1. 访问：https://github.com/yuyu0308/one
2. 点击 `data.json` 文件
3. 点击编辑按钮（铅笔图标）
4. 修改内容
5. 滚动到页面底部，点击"Commit changes"
6. 1-2分钟后网站自动更新

## 🚀 如何推送更新到GitHub

### 快速推送

```bash
cd C:\Users\lenovo\personal-website
git add .
git commit -m "更新网站内容"
git push
```

### 使用批处理文件

双击运行：`C:\Users\lenovo\personal-website\push.bat`

## 📁 项目结构

```
personal-website/
├── index.html          # 主页
├── edit.html           # 编辑器
├── data.json           # 网站数据（核心文件）
├── static/
│   ├── css/
│   │   ├── style.css   # 主页样式
│   │   └── admin.css   # 编辑器样式
│   └── js/
│       └── main.js     # 主页脚本
└── README.md           # 本说明文件
```

## 🎨 自定义内容

### 修改个人信息

编辑 `data.json` 中的 `profile` 部分：

```json
{
  "profile": {
    "name": "你的名字",
    "title": "前端开发者",
    "bio": "你的个人简介",
    "email": "your.email@example.com",
    "github": "https://github.com/yourusername",
    "avatar": "你的头像URL"
  }
}
```

### 添加技能

在 `data.json` 的 `skills` 数组中添加：

```json
{
  "skills": [
    {"name": "HTML/CSS", "level": 90},
    {"name": "JavaScript", "level": 85},
    {"name": "新技能", "level": 80}
  ]
}
```

### 添加项目

在 `data.json` 的 `projects` 数组中添加：

```json
{
  "projects": [
    {
      "id": "项目ID",
      "title": "项目名称",
      "description": "项目描述",
      "image": "项目图片URL",
      "link": "项目链接",
      "tags": ["标签1", "标签2"]
    }
  ]
}
```

### 添加文件

在 `data.json` 的 `files` 数组中添加：

```json
{
  "files": [
    {
      "name": "文件名",
      "description": "文件描述",
      "url": "文件下载链接",
      "type": "pdf",
      "size": 1024000
    }
  ]
}
```

## 🖼️ 使用图片

### 头像和项目图片

推荐使用免费图床：

1. **ImgBB**: https://imgbb.com
2. **图壳**: https://imgkr.com
3. **路过图床**: https://imgtp.com

步骤：
1. 上传图片到图床
2. 复制图片链接
3. 粘贴到 `data.json` 对应位置

### 文件下载链接

可以使用：
1. **GitHub Releases**: 上传文件到GitHub Releases获取下载链接
2. **百度网盘/蓝奏云**: 创建分享链接
3. **其他云存储**: 阿里云OSS、七牛云等

## 🎨 自定义主题

编辑 `data.json` 中的 `theme` 部分：

```json
{
  "theme": {
    "background_type": "gradient",  // gradient(渐变) | solid(纯色) | image(图片)
    "background_color": "#667eea",   // 渐变起始色或纯色
    "background_color_end": "#764ba2", // 渐变结束色
    "background_image": "图片URL",     // 背景图片URL
    "cursor_style": "default"          // default | pointer | custom
  }
}
```

## 📊 查看访问统计

使用免费的统计服务：

1. **百度统计**: https://tongji.baidu.com
2. **Google Analytics**: https://analytics.google.com
3. **51.LA**: http://www.51.la

将统计代码添加到 `index.html` 的 `<head>` 标签中。

## 🔧 常见问题

### Q: 编辑后网站没有更新？

A: 
1. 检查是否已推送到GitHub
2. 等待1-2分钟，Vercel需要时间自动部署
3. 刷新浏览器缓存（Ctrl+F5）

### Q: 图片显示不出来？

A: 
1. 确保图片URL是公开可访问的
2. 检查URL是否正确
3. 尝试使用其他图床

### Q: 如何修改网站标题？

A: 编辑 `index.html` 中的 `<title>` 标签。

## 📞 获取帮助

如果遇到问题：
1. 检查浏览器控制台（F12）的错误信息
2. 查看GitHub仓库的Actions标签查看部署状态
3. 访问Vercel控制台查看部署日志

---

**祝你使用愉快！** 🎉